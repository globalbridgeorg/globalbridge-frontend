<script setup>
</script>

<!--
Prompt rápido para rodar localmente as verificações e testes após minhas alterações.

Copie e cole no seu terminal na pasta `gbfrontend`:

1) Instalar dependências (se necessário):
	npm install

2) Rodar lint e checar erros:
	npm run lint

3) Rodar aplicação em modo dev:
	npm run dev

4) Build de produção (opcional):
	npm run build

5) Para checar se o front comunica com o backend local, abra outra aba e rode o backend:
	cd ../gbbackend && ./build.sh  # ou python manage.py runserver

Observações:
- Se usar autenticação, limpe `localStorage`/`sessionStorage` antes se quiser testar fluxo de login do zero.
- Se perceber leaks/saltos de memória, abra DevTools (Performance/Memory) e faça um snapshot.

Prompt de commit rápido (opcional):
	git add . && git commit -m "Refactor: compact LoginView + add NotificationBanner" && git push
-->
</script>

<template>
</template>
</template>

<style scoped>

</style>